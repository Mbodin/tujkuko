
(library
	(public_name tujkuko)
	(name tujkuko)
	(libraries libutils extlib yojson lwt uutf re)
	(preprocess (pps lwt_ppx ppx_deriving ppx_inline_test))
	(modules_without_implementation recipe)
	(modes byte))

(env (dev (flags (:standard -w -50))))

